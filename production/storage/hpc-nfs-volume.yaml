apiVersion: v1
kind: PersistentVolume
metadata:
  name: hpc-nfs
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  nfs: 
    server: nfs-server.default.svc.cluster.local
    path: "/k8sdata"
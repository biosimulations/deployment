apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: biosimulations-api-secrets
    namespace: default
    files:
      - biosimulations-api/secret.env
  - name: account-api-secrets
    namespace: default
    files:
      - account-api/secret.env
  - name: dispatch-api-secrets
    namespace: default
    files:
      - dispatch-api/secret.env
  - name: dispatch-service-secrets
    namespace: default
    files:
      - dispatch-service/secret.env

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: biosimulators-api
  name: biosimulators-api
  namespace: default
spec:
  selector:
    matchLabels:
      app: biosimulators-api
  replicas: 3
  template:
    metadata:
      labels:
        app: biosimulators-api
    spec:
      containers:
        - name: biosimulators-api
          image: biosimulations/biosimulators-api:dev
          imagePullPolicy: "Always"
          volumeMounts:
            - mountPath: "/app/secret"
              name: secret
              readOnly: true
          ports:
            - containerPort: 3333
      volumes:
        - name: secret
          secret:
            secretName: biosimulators-api-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: biosimulators-api-service
  namespace: default
  labels:
    app: biosimulators-api
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3333
  selector:
    app: biosimulators-api
  type: NodePort

apiVersion: apps/v1
kind: Deployment
metadata:
  name: run-api
  labels:
    app: run-api
  namespace: default
spec:
  selector:
    matchLabels:
      app: run-api
  replicas: 1
  template:
    metadata:
      labels:
        app: run-api
    spec:
      securityContext:
        runAsUser: 16997
        runAsGroup: 10000
        fsGroup: 16521
      volumes:
        - name: secret
          secret:
            secretName: dispatch-api-secrets
        - name: task-pv-storage
          nfs:
            server: uchc-app.bioscience-ct.net
            path: /projects/CRBM/biosimulations
      containers:
        - name: run-api
          image: "ghcr.io/biosimulations/dispatch-api:alpha"
          imagePullPolicy: "Always"
          ports:
            - containerPort: 3333
          volumeMounts:
            - mountPath: /app/secret
              name: secret
              readOnly: true
            - mountPath: /sharedFiles
              name: task-pv-storage
---
apiVersion: v1
kind: Service
metadata:
  name: run-api
  labels:
    app: run-api
  namespace: default
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3333
      protocol: TCP
      name: run-api
  selector:
    app: run-api

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx

  name: dev-data-service-ingress
  labels:
    name: dev-data-service-ingress
spec:
  rules:
  - host: data.biosimulations.dev
    http:
      paths:
      - pathType: Prefix
        path: "/domain"
        backend:
          service:
            name: hsds
            port: 
              number: 80              
      - pathType: Prefix
        path: "/groups"
        backend:
          service:
            name: hsds
            port: 
              number: 80              
      - pathType: Prefix
        path: "/datasets"
        backend:
          service:
            name: hsds
            port: 
              number: 80              
      - pathType: Prefix
        path: "/acls"
        backend:
          service:
            name: hsds
            port: 
              number: 80           
      - pathType: Prefix
        path: "/datatypes"
        backend:
          service:
            name: hsds
            port: 
              number: 80              

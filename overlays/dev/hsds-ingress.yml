apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx

  name: dev-hsds-ingress
  labels:
    name: dev-hsds-ingress
spec:
  tls:
    - hosts:
      - data.biosimulations.dev
      secretName: dev-hsds-ingress-tls
  rules:
  - host: data.biosimulations.dev
    http:
      paths:
      - pathType: Prefix
        path: "/domain"
        backend:
          service:
            name: hsds
            port: 
              number: 80              
      - pathType: Prefix
        path: "/groups"
        backend:
          service:
            name: hsds
            port: 
              number: 80              
      - pathType: Prefix
        path: "/datasets"
        backend:
          service:
            name: hsds
            port: 
              number: 80              
      - pathType: Prefix
        path: "/acls"
        backend:
          service:
            name: hsds
            port: 
              number: 80           
      - pathType: Prefix
        path: "/datatypes"
        backend:
          service:
            name: hsds
            port: 
              number: 80              

apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/proxy-body-size: "600m"
    nginx.org/client-max-body-size: "600m"
  name: dev-biosimulations-ingress

spec:
  tls:
    - hosts:
        - account.api.biosimulations.dev
        - api.biosimulations.dev        
        - data.biosimulations.dev

      secretName: biosimulations-dev-ingress-certificate

  rules:
    - host: account.api.biosimulations.dev
      http:
        paths:
          - backend:
              serviceName: account-api
              servicePort: 80

    - host: api.biosimulations.dev
      http:
        paths:
          - backend:
              serviceName: api
              servicePort: 80
            path: /runs
          - backend:
              serviceName: api
              servicePort: 80
            path: /metadata
          - backend:
              serviceName: api
              servicePort: 80
            path: /results
          - backend:
              serviceName: api
              servicePort: 80
            path: /logs
          - backend:
              serviceName: api
              servicePort: 80
            path: /files
          - backend:
              serviceName: api
              servicePort: 80
            path: /specifications
          - backend:
              serviceName: api
              servicePort: 80
            path: /projects
          - backend:
              serviceName: api
              servicePort: 80
            path: /ontologies
          - backend:
              serviceName: api
              servicePort: 80
            path: /health
          - backend:
              serviceName: api
              servicePort: 80
            path: /
                          

    - host: data.biosimulations.dev
      http:
        paths:
          - backend:
              serviceName: hsds
              servicePort: 80
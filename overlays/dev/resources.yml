apiVersion: apps/v1
kind: Deployment
metadata:
  name: dispatch-api
spec:
  template:
    spec:
      containers:
        - resources:
            requests:
              memory: "4Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "500m"

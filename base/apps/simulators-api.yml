apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: simulators-api
  name: simulators-api
  
spec:
  selector:
    matchLabels:
      app: simulators-api
  replicas: 3
  template:
    metadata:
      labels:
        app: simulators-api
    spec:
      containers:
        - name: simulators-api
          image: biosimulations/simulators-api
          imagePullPolicy: "Always"
          volumeMounts:
            - mountPath: "/app/secret"
              name: secret
              readOnly: true
          ports:
            - containerPort: 3333
      volumes:
        - name: secret
          secret:
            secretName: simulators-api-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: simulators-api
  labels:
    app: simulators-api
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3333
  selector:
    app: simulators-api
  type: NodePort
